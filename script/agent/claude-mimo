#!/usr/bin/env bash
# Convenience launcher: Claude Code through the Xiaomi MiMo entrypoint.
#
# Usage:
#   claude-mimo [claude args...]

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
exec "$script_dir/mimo" claude "$@"
