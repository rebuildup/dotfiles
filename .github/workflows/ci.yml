name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - 'release-*'

permissions:
  contents: read

jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - name: Check shell syntax
        run: bash -n script/link script/check script/test script/secrets-init script/secrets-doctor script/with-secrets script/check-secrets
      - name: Test symlink behavior
        run: ./script/test
      - name: Guard secret material
        run: ./script/check-secrets
